% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-mapping.R
\name{select}
\alias{select}
\alias{select,GeneslatorDb-method}
\title{Extract data from the annotation databases of geneslator}
\usage{
\S4method{select}{GeneslatorDb}(
  x,
  keys,
  columns,
  keytype,
  search.aliases = TRUE,
  search.archives = TRUE,
  orthologs.mapping = "multiple",
  ...
)
}
\arguments{
\item{x}{A \code{GeneslatorDb} object returned by \code{\link[=GeneslatorDb]{GeneslatorDb()}}.
It represents the annotation database to query from.}

\item{keys}{Values used as keys to retrieve records from the annotation
database.}

\item{columns}{Columns to return as output of the query. See \code{\link[=columns]{columns()}}
for more details.}

\item{keytype}{Column representing the type of values of \code{keys} parameter.
See \code{\link[=keytypes]{keytypes()}} for more details.}

\item{search.aliases}{When no mapping is found using gene symbol (SYMBOL
column), should \code{select} perform query using also ALIAS column? (default =
TRUE). This parameter is used only in queries involving SYMBOL column.}

\item{search.archives}{When no mapping is found using NCBI gene ids
(ENTREZID column) and/or Ensembl gene ids (ENSEMBL column), should \code{select}
perform query using also archived identifiers (columns ENTREZIDOLD and/or
ENSEMBLOLD)? (default = TRUE). This parameter is used only in queries
involving ENTREZID and/or ENSEMBL column.}

\item{orthologs.mapping}{Return all orthologs (\code{"multiple"}) or just the
first ortholog (\code{"single"}) of a gene? (default = \code{"multiple"}). Used only
in queries where the output columns include \code{ORTHO} columns (e.g.
ORTHOMOUSE, ORTHOYEAST).}

\item{...}{Other arguments. See
\code{\link[AnnotationDbi:AnnotationDb-class]{AnnotationDb}} for more info.}
}
\value{
\code{select} returns a dataframe with all columns specified by
\code{keytype} and \code{columns} parameters and one row for each mapping
found between keys and column values.
}
\description{
\code{select} query annotation databases of \pkg{geneslator} package, by mapping
different types of gene annotation data from several source of data.
}
\details{
\code{select} collects all possible mappings between values of the
column specified by \code{keytype} parameter and values of the columns specified
by the \code{columns} parameter.
}
\examples{
#Lookup NCBI gene ids for a given list of gene symbols in fly
fly.db <- GeneslatorDb("Fly")
geneslator::select(fly.db, keys=c("CG14883","GstE2"), columns="ENTREZID", 
keytype="SYMBOL")

# Lookup KEGG pathway ids and their relative full names for a given list 
# of ensembl gene ids in worm
worm.db <- GeneslatorDb("Worm")
geneslator::select(worm.db, keys=c("ENSDARG00000013522",
"ENSDARG00000103044"), columns=c("KEGGPATH","KEGGPATHNAME"), 
keytype="ENSEMBL")

# Lookup mouse orthologs for a list of human gene symbols. 
# Ignore aliases and return only the first ortholog found for each gene
human.db <- GeneslatorDb("Human")
geneslator::select(human.db, keys=c("BRCA1","PTEN"), columns="ORTHOMOUSE", 
keytype="SYMBOL", search.aliases = FALSE, orthologs.mapping = "single")

# Lookup gene ontologies for a list of entrez ids in arabidopsis. 
# Do not use NCBI archive data
arabidopsis.db <- GeneslatorDb("Arabidopsis")
geneslator::select(arabidopsis.db, keys=c("820005","831939"), 
columns=c("GO","GONAME","GOTYPE"), keytype="ENTREZID", 
search.archives = FALSE)

}
\seealso{
\code{\link{availableOrganisms}}, \code{\link{keytypes}},
\code{\link{columns}}
}
