% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-mapping.R
\name{mapIds}
\alias{mapIds}
\alias{mapIds,GeneslatorDb-method}
\title{Map data from the annotation databases of geneslator}
\usage{
\S4method{mapIds}{GeneslatorDb}(
  x,
  keys,
  column,
  keytype,
  search.aliases = TRUE,
  search.archives = TRUE,
  ...,
  multiVals
)
}
\arguments{
\item{x}{A \code{GeneslatorDb} object returned by \code{\link[=GeneslatorDb]{GeneslatorDb()}}.
It represents the annotation database to query from.}

\item{keys}{Values used as keys to retrieve records from the annotation
database.}

\item{column}{Column to return as output of the query. See \code{\link[=columns]{columns()}}
for more details.}

\item{keytype}{Column representing the type of values of \code{keys} parameter.
See \code{\link[=keytypes]{keytypes()}} for more details.}

\item{search.aliases}{When no mapping is found using gene symbol (SYMBOL
column), should \code{select} perform query using also ALIAS column? (default =
TRUE). This parameter is used only in queries involving SYMBOL column.}

\item{search.archives}{When no mapping is found using NCBI gene ids
(ENTREZID column) and/or Ensembl gene ids (ENSEMBL column), should \code{select}
perform query using also archived identifiers (columns ENTREZIDOLD and/or
ENSEMBLOLD)? (default = TRUE). This parameter is used only in queries
involving ENTREZID and/or ENSEMBL column.}

\item{...}{Other arguments. See
\code{\link[AnnotationDbi:AnnotationDb-class]{AnnotationDb}} for more info.}

\item{multiVals}{What should \code{mapIds} do when there are multiple output
values that could be returned for a specific input? Options include:\tabular{ll}{
   Option \tab Description \cr
   \code{first} \tab Return a vector object containing only the first match found \cr
   for each input (default behaviour). \tab  \cr
   \code{asNA} \tab Return a vector object with \code{NA} values whenever there are \cr
   multiple matches for a given input. \tab  \cr
   \code{filter} \tab Return a shorter vector object, excluding all inputs for which \cr
   multiple matches have been found. \tab  \cr
   \code{list} \tab Return a list object with all matches found for each input. \cr
   \code{CharacterList} \tab Return a \code{SimpleCharacterList} object with all matches \cr
   found for each input. \tab  \cr
   \code{FUN} \tab Supply a function to the \code{multiVals} argument for custom \cr
   behaviors. \tab  \cr
}


If using \code{FUN}, the function must take a single argument and return a single
value. This function will be applied to all elements and will serve as a
'rule' for which item to keep when there is more than one match for a given
input. For example, the following function grabs the last element in each
result: \code{last <- function(x) { x[[length(x)]] }}.}
}
\value{
\code{mapIds} returns either a named vector, where each value is a possible
mapping (if exists) for a given key, or a list of values, where each element
of the list is the vector of all mappings found for a given key. The type of
the return object depends on the value of the \code{multiVals} parameter.
}
\description{
\code{mapIds} maps key values of a column to values of another column in the
annotation databases of \pkg{geneslator} package.
}
\details{
\code{mapIds} maps each key value to either a single value or a list of
values of the type specified by \code{column} parameter, depending on the
value of \code{multiVals} parameter.
}
\examples{
# Map NCBI gene ids to gene aliases in yeast. 
# Return a named vector with 1st mapping found
yeast.db <- GeneslatorDb("Yeast")
geneslator::mapIds(yeast.db, keys=c("856781","1466469"), column="ALIAS", 
keytype="ENTREZID")

# Map gene symbols to gene ontologies in mouse. 
# Return a list with all possible mappings
mouse.db <- GeneslatorDb("Mouse")
geneslator::mapIds(mouse.db, keys=c("Grin2a","Rev3l"), column="GO", 
keytype="SYMBOL", multiVals="list")

# Map gene symbols to uniprot ids in rat. Apply a custom function to 
# return the last mapping found and do not use Ensembl archive data.
rat.db <- GeneslatorDb("Rat")
last <- function(x){x[[length(x)]]}
geneslator::mapIds(rat.db, keys=c("ENSRNOG00000003105","ENSRNOG00000049505"),
column="UNIPROT", keytype="ENSEMBL", multiVals="list", search.archives=FALSE)

# Map gene symbols to reactome pathways in zebrafish.
# Return a CharacterList object with all possible mappings
zebrafish.db <- GeneslatorDb("Zebrafish")
geneslator::mapIds(zebrafish.db, keys=c("hoxc8a","samhd1"), 
column="REACTOMEPATH", keytype="SYMBOL", multiVals="CharacterList")

}
\seealso{
\code{\link{availableOrganisms}}, \code{\link{keytypes}},
\code{\link{columns}}
}
